# Google Drive連携の設定

## 1. Google側の設定（本人の操作）

1. https://console.cloud.google.com/ を開き、保存に使うGoogleアカウントでログインします。
2. プロジェクト選択から「新しいプロジェクト」を作成します。名前は「SyukatsuOS」など。
3. 「APIとサービス → ライブラリ」で「Google Drive API」を検索し、有効にします。
4. 「Google Auth Platform」（またはOAuth同意画面）を開き、開始します。アプリ名「就活OS」、連絡先には自分のメールを設定します。
5. AudienceはExternalにし、Testingで自分のGoogleメールをテストユーザーに追加します。
6. Data Accessで `https://www.googleapis.com/auth/drive.file` を追加します。Drive全体へのアクセス権限は不要です。
7. ClientsからOAuthクライアントを作成します。種類は「ウェブアプリケーション」。
8. 「承認済みのJavaScript生成元」に以下を追加します（末尾スラッシュやアプリのパスを付けません）。
   - `https://syuhaya2026-web.github.io`
   - `http://127.0.0.1:8765`
   - `http://localhost:8765`（こちらで開く場合）
9. この方式ではリダイレクトURIは不要です。作成後の「クライアントID」（末尾が `.apps.googleusercontent.com`）をコピーします。**クライアントシークレットはアプリに入力・公開しません。**

画面名はGoogle側の更新で変わる場合があります。設定が反映されるまで数分かかることがあります。

## 2. アプリを接続

1. 就活OSの右上「Drive 未接続」を押します。
2. OAuthクライアントIDを入力し「Googleに接続」を押します。
3. 自分のGoogleアカウントを選び、権限を許可します。
4. Driveのマイドライブに「就活OS」フォルダが自動作成されます。
5. iPhoneとMacでも**同じクライアントID・同じGoogleアカウント**で接続します。

クライアントIDは端末に保存されます。アクセストークンはメモリ内だけに保持します。アプリを開き直した場合や認証期限が切れた場合は接続ボタンから再認証が必要です。認証中は保存直後、表示復帰時、10秒ごとに同期します。アプリを閉じている間の同期は行いません。

## 現在の保存方式（v2）

- 通常の同期は企業ごとのJSONを上書きします。その企業の進捗・予定も同じファイルに含みます。編集ごとに全体履歴を追加しません。
- 添付本体は独立した共通JSONへ一度保存し、企業ファイルは添付IDで参照します。JSON内ではBase64なので、Driveで画像そのもののプレビューはできません。
- 保存直後に送信し、アプリが表示されている間は10秒ごと・表示復帰時にも変更を確認します。通信中の追加変更は続けて送ります。編集中のフォームが開いている間は受信内容の適用を待ちます。
- 同じ企業への更新が重なる場合、Driveの更新条件を確認して書き込みます。相手が更新済みなら再読込し、項目ごとに統合します。同じ項目の異なる変更と削除対編集だけ選択します。
- 削除した企業のファイルは小さな削除記録として残します。他端末の古いデータから勝手に復活しないためです。添付実体の自動整理はしません。
- 全体データ・バックアップは添付込み20MiBまでです。大量の画像向けの構成ではありません。
- ブラウザで安全な条件付き更新を利用できるか、初回移行時に専用の準備ファイルで検査します。失敗した場合は新方式を有効にせず、既存データを残して停止します。エラー文を控えてください。

## 個人データの保存先

Google Driveとこの端末のIndexedDBです。個人データをGitHubへ送信する処理はありません。Google認証ライブラリはGoogleから読み込みます。Driveに置くJSONは暗号化バックアップではありません。フォルダを他人に共有しないでください。

## 確認手順

最初はテスト企業で、Macから保存→iPhoneで読込→iPhoneで編集→Macで反映を確認してください。その後、機内モードで編集→オンラインで反映、画像添付、二端末同時編集の競合表示も確認します。

## 公式資料

- https://developers.google.com/identity/oauth2/web/guides/use-token-model
- https://developers.google.com/workspace/drive/api/guides/api-specific-auth
- https://developers.google.com/workspace/drive/api/guides/manage-uploads

実Googleアカウントでの接続・二端末動作確認は、クライアントID作成後に行う必要があります。

## 起動時の再接続と点検修正

クライアントID設定済みの場合、起動時に「最新の記録で始める」を表示します。「Googleで続ける」で認証・同期へ進みます。端末だけで使う選択も可能です。完全に無操作の再認証ではなく、Googleの画面で追加操作が必要な場合もあります。

ズーム抑止はviewport・touch-action・iOSジェスチャー制御で実施します。OSのアクセシビリティ拡大やブラウザの拡大機能を完全に制限するものではありません。実iPhoneでの確認は別途必要です。

Driveデータの形式・参照を検証し、1応答20MiB、1回の読込100MiBを超えたら同期を停止します。旧方式からの移行は旧履歴一覧500件、新方式の一覧は10,000件までです。履歴を手動削除せず整理対応を依頼してください。送信中に追加編集した場合は未同期を維持します。

同じGitHub Pagesホストの別アプリとは保存領域が隔離されません。重要情報の唯一の保管先・一般販売用としては、専用オリジンや独立した復元機能などの改善が必要です。

## 項目ごとの自動統合・競合の解決

- MacでA社、iPhoneでB社を編集：両方を自動で取り込みます。
- 同じ企業の「企業名」と「メモ」を別端末で編集：両方を取り込みます。
- 同じ進捗の「タイトル」を別々の内容に編集：企業名・記録名・項目名・端末名・候補を表示します。残す候補を選んで「選んだ項目を反映して統合」を押してください。他の項目の変更も取り込みます。
- 添付の追加は独立したIDで管理し、両方を保持します。同名ファイルでも別IDなら別ファイルです。
- 一方で削除、他方で編集：削除するか記録を保持するかを確認します。企業を削除する選択は、その企業の進捗・予定も削除します。保持した企業の子項目にも競合があれば追加で選択します。
- 同じ値への変更や、更新日時だけの違いでは選択を求めません。
- 選択中に別端末の更新が届いた場合は、同期し直して再確認します。

Drive設定の「この端末の名前」を「Mac」「iPhone」などに設定できます。候補の端末名は、その保存履歴を作成した端末です。旧版の履歴は「旧版の端末」と表示します。統合済み履歴は統合した端末名になり、個々の文字を最初に編集した端末まで追跡するものではありません。

同期原本は「企業データ」の企業別JSON、添付本体は「添付データ」のJSONです。復元用の全体バックアップは別途作成します。条件付き更新に失敗した場合は再読込して統合し、通信失敗では端末の変更を未同期として残します。

## Driveで企業ごとの記録を読む

接続・同期後、次の閲覧用ファイルを自動作成します。

```text
マイドライブ / 就活OS /
  企業データ/企業名__ID.json       ← 最新の企業・進捗・予定
  添付データ/添付名__ID.json       ← 添付本体（Base64）
  全体バックアップ/全体_日時.json  ← 添付込みの復元データ
  同期管理.json                   ← 管理情報。直接編集しない
  記録_日時_ID.json                ← 移行前の旧履歴。自動削除しない
  企業別ノート（閲覧用）/
    A株式会社__企業ID.txt
    B株式会社__企業ID.txt
```

テキストには現在のステータス、My Page、企業サイト、企業メモ、日付順の選考タイムライン、予定、添付ファイル名をまとめます。会社名の後のIDは同名企業の区別用です。変更がある企業のテキストを同じDriveファイルへ上書きし、企業名変更時はファイル名も更新します。アプリで企業を削除した場合は「削除済み」の案内に置き換えます。

**ノートは閲覧用です。編集はアプリから行ってください。** Driveでノートを編集してもアプリへ取り込まれず、アプリの変更によって上書きされます。画像本体はノートに埋め込まず、添付ファイル名のみ記載します。このテキストだけでは添付込みの復元はできません。

ノート更新だけが失敗した場合、Drive設定に「記録は保存済みですが、企業別ノートは未更新」と表示します。次回同期で再試行します。同時更新中はノート表示が一時的に遅れる場合があります。ノートも個人情報を含むのでフォルダを他人へ共有しないでください。

両端末で更新版を再読み込みしてから、テスト企業で別項目の同時編集と同じタイトルの同時編集を一度確認してください。古い版の端末は全体選択の旧方式で動作するため、混在させないでください。

## 初回の切り替え

1. 可能なら旧版で両端末を同期済みにし、両方を閉じます。
2. **iPhoneとMacの両方を更新版で再読み込み**します。まず1台をGoogle Driveに接続してください。
3. 旧方式の未同期分を統合し、条件付き更新の検査と移行前全体バックアップを行います。「移行中」はアプリを閉じずに待ってください。
4. 企業別データを準備し、バックアップを読み返して確認してから新方式に切り替えます。元の履歴は削除しません。
5. 「同期済み」になったら、もう1台も接続します。旧版の同期基準が残っていれば、オフライン中の編集も比較して取り込みます。

途中で閉じた場合は、次の接続時に準備中の情報を確認して再試行します。移行後に旧版から履歴が追加された場合、黙って無視せず同期を止めます。両端末を更新し、元データを削除せず対応を依頼してください。条件付き更新の検査が失敗した場合も、エラー表示を共有してください。

旧「記録_...json」は移行と未更新端末の比較用に保持し、30日の自動整理対象にはしません。初回の中断や同時移行によって、利用されない準備ファイル・フォルダが残る場合があります。手動で同期管理や企業ファイルを消さないでください。

## 自動バックアップと復元

- 初回移行時に全体バックアップを1回作ります。次回は最後の成功から7日後です。
- 予定日に起動していなければ、次に起動して認証・同期が完了したときに作ります。アプリを閉じている間の定時実行は保証しません。
- 企業・進捗・予定・添付本体を含めて保存し、読み返して内容を検証します。作成中は「完了までアプリを閉じないでください」と表示します。閉じる操作そのものは防げません。
- 途中で閉じたり通信が失敗したりした場合、未確認のファイルは成功扱いにしません。次回に再試行します。
- 作成から30日を超えた古いバックアップをゴミ箱へ移します。最後の正常な1件は必ず残します。整理中に中断した場合も、記録した整理待ち一覧から再開します。
- 両端末が同時に作成した場合、管理情報への登録は1回分だけにします。中断時に残った未登録のバックアップも30日を超えたものから整理します。
- Driveのゴミ箱に残っている分は容量を使う場合があります。完全削除はDrive側の処理に従います。

**復元方法：右上のDrive → 全体バックアップ →「内容・復元」→ 内容・日時を確認 →「この内容に復元する」。**

復元前にも現在の全体を保存します。復元は企業・進捗・予定・添付参照を選んだ時点へ戻し、その結果を他端末にも同期します。他端末の未同期編集がある場合は通常どおり統合・競合確認を行います。添付実体は未参照になっても保持します。Googleカレンダーに追加済みの予定はアプリの復元では変わりません。

バックアップ作成に失敗しても通常の同期が成功していれば「同期済み・バックアップ要確認」と表示します。最新データの同期と、過去に戻せるバックアップの成功は別々に確認してください。


### 更新情報の取得方法の修正

Drive v3のHTTPヘッダーにETagがある前提を修正しました。条件付き更新に使う情報は、Drive v2のFileリソースのJSON内 `etag` から取得し、更新もv2で行います。一覧・新規作成・内容の読込はv3を使用します。同じGoogleアカウント・同じ `drive.file` 権限で動作し、Google側の設定変更は不要です。ETagなし・古いETagの拒否検査は維持します。

公式資料：https://developers.google.com/workspace/drive/api/reference/rest/v2/files


### 「旧版のアプリから更新が届いています」で止まった場合

新版では、旧形式の「記録_…json」が移行後に届いた場合も、共通履歴を使って企業別データへ取り込みます。異なる項目の編集は統合し、同じ項目の食い違いは選択画面で確認します。その後で端末内の未同期編集を比較・同期するため、取り込みだけで端末データを置き換えることはありません。

まずMac・iPhoneのアプリを最新版に更新してください。未同期情報が残るMacを先に接続し、「同期済み」を確認してからiPhoneを接続します。取り込み中はアプリを開いたままにしてください。サイトデータの削除、アプリの再インストール、古いバックアップへの復元は復旧手順ではありません。

取り込み前の全体バックアップを作成し、元の記録・企業ファイルを残したまま新しい企業ファイルを作成・読み戻して検証します。最後に同期管理ファイルを条件付きで切り替えます。この復旧では一時的に同名の企業ファイルが増えることがあります。元ファイルは削除しないでください。以降の通常編集は利用中の企業ファイルを更新します。

初回移行バックアップの欠落、過去の記録の編集・削除、元ファイルへの遅れた書き込みなどで比較基準を確認できない場合は、自動取り込みを停止します。その場合も元データを消さず、表示されたメッセージを確認してください。実際のアカウントでの復旧完了は、両端末で企業情報を確認して判断してください。
